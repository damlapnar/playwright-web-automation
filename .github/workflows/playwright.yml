name: Playwright Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 8 * * 1-5'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - project: chromium
            installBrowser: chromium
          - project: firefox
            installBrowser: firefox
          - project: webkit
            installBrowser: webkit
          - project: mobile-chrome
            installBrowser: chromium
          - project: mobile-safari
            installBrowser: webkit

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Install Playwright browsers
        run: npx playwright install --with-deps ${{ matrix.installBrowser }}

      - name: Run Playwright tests
        run: npx playwright test --project=${{ matrix.project }}
        env:
          BASE_URL: https://www.saucedemo.com

      - name: Write step summary
        if: always()
        run: |
          if [ -f test-results/results.json ]; then
            PASSED=$(cat test-results/results.json | python3 -c "import sys,json; r=json.load(sys.stdin); print(sum(1 for s in r.get('suites',[]) for t in s.get('specs',[]) if t.get('ok')))" 2>/dev/null || echo "?")
            FAILED=$(cat test-results/results.json | python3 -c "import sys,json; r=json.load(sys.stdin); print(sum(1 for s in r.get('suites',[]) for t in s.get('specs',[]) if not t.get('ok')))" 2>/dev/null || echo "?")
            echo "## Playwright Results — ${{ matrix.project }}" >> $GITHUB_STEP_SUMMARY
            echo "| Status | Count |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| ✅ Passed | $PASSED |" >> $GITHUB_STEP_SUMMARY
            echo "| ❌ Failed | $FAILED |" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload test report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-${{ matrix.project }}
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 30

      - name: Generate Allure report
        if: always()
        run: npx allure generate allure-results --clean -o allure-report

      - name: Upload Allure report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-report-${{ matrix.project }}
          path: allure-report/
          if-no-files-found: ignore
          retention-days: 30
